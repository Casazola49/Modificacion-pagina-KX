
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Permite la lectura pública de todos los archivos
    match /{allPaths=**} {
      allow read;
      // Permite la escritura si el usuario está autenticado
      allow write: if request.auth != null;
    }
  }
}
